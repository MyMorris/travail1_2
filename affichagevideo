import processing.video.*;
Movie ecrantv;

void setup(){
  size(1920,1080);
  ecrantv= new Movie(this,"video.mov");
  ecrantv.loop();}
  
  void movieEvent(Movie ecrantv)
  {ecrantv.read();}
  
  void draw(){
    image(ecrantv,0,0,width,height);}
